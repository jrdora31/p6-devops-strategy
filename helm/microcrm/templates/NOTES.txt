1. Vérifiez les Pods :
   kubectl get pods -n {{ .Release.Namespace }}

{{- if .Values.ingress.enabled }}
2. L'Ingress attend l'hôte {{ .Values.ingress.host }}.
   Vérifiez son état :
   kubectl get ingress -n {{ .Release.Namespace }}
{{- else }}
2. Ouvrez un port-forward :
   kubectl port-forward -n {{ .Release.Namespace }} service/{{ include "microcrm.fullname" . }}-frontend 8080:80

3. Ouvrez http://127.0.0.1:8080
{{- end }}
