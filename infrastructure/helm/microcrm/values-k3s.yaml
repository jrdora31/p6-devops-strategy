# Surcharge générique pour un cluster K3s utilisant Traefik et local-path.
database:
  persistence:
    size: 2Gi
    storageClass: local-path

ingress:
  enabled: true
  className: traefik
  host: microcrm.example.invalid
